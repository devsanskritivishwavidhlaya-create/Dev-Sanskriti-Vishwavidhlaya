server {
    listen 80;
    listen [::]:80;
    server_name dsvv.ac.in www.dsvv.ac.in;

    root /var/www/dsvv.ac.in;
    index index.html;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
    gzip_min_length 256;
    gzip_vary on;

    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Don't cache HTML
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Main location
    location / {
        try_files $uri $uri/ =404;
    }

    # Custom error pages
    error_page 404 /index.html;
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /var/www/dsvv.ac.in;
    }

    # Deny access to sensitive files
    location ~ /\. {
        deny all;
    }

    location ~* Login\ Details\.txt$ {
        deny all;
    }
}

# HTTPS server (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name dsvv.ac.in www.dsvv.ac.in;
#
#     ssl_certificate /etc/letsencrypt/live/dsvv.ac.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dsvv.ac.in/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     root /var/www/dsvv.ac.in;
#     index index.html;
#
#     # Same config as above...
# }
